export default function App() {
  return (
    <main className="app">
      <section className="hero" aria-labelledby="app-title">
        <p className="eyebrow">Personal React notes</p>
        <h1 id="app-title">React Cheat Sheet</h1>
        <p className="intro">
          A focused place for React rules, examples, snippets, and interview
          answers from course practice.
        </p>
      </section>
    </main>
  );
}

